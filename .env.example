# IziBlog Environment Variables
# Copy this file to .env and update the values.

# Public site URL (used for canonical URLs, OG, RSS, sitemap)
PUBLIC_SITE_URL=https://example.com

# Optional: Analytics ID (only loads after user consent if configured)
# PUBLIC_ANALYTICS_ID=
